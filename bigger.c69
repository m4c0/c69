extern fn int    close(int fd);
extern fn void   exit (int code);
extern fn int    open (const char * fn, int mode);
extern fn size_t read (int fd, void * ptr, size_t len);
extern fn size_t write(int fd, const void * ptr, size_t len);
